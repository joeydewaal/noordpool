# Copy to frontend/.env to override the default API base URL.
# Leave unset for local dev — Vite proxies /api to http://localhost:3000.
# Set in CF Pages build env to point at the deployed backend:
# VITE_API_BASE_URL=https://api.noordpool.joeydewaal.com
